apiVersion: v1
kind: ServiceAccount
metadata:
  name: support-bundle-sa
  namespace: qdrant
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: support-bundle-role
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list"]
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get"]
  - apiGroups: [""]
    resources: ["pods/portforward"]
    verbs: ["create"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: support-bundle-rb
subjects:
  - kind: ServiceAccount
    name: support-bundle-sa
    namespace: qdrant
roleRef:
  kind: ClusterRole
  name: support-bundle-role
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: support-bundle
  namespace: qdrant
spec:
  replicas: 1
  selector:
    matchLabels:
      app: support-bundle
  template:
    metadata:
      labels:
        app: support-bundle
    spec:
      serviceAccountName: support-bundle-sa
      containers:
        - name: bundle
          image: zettertty/qdrant-support-bundle:v0.20
          imagePullPolicy: IfNotPresent